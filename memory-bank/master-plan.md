# Master Plan

## Milestones
1. Confirm product stack and architecture direction.
2. Complete Tauri + SvelteKit + TypeScript scaffold baseline.
3. Validate polling, persistence, and OTP extraction behavior.
4. Build production desktop app per PRD/API contracts.

## Upcoming Work
- Implement full Mail.tm + phone pool workflow screens and IPC integration.
- Build production message lists, detail panes, and refresh controls.
- Add persisted settings and event caching across sessions.
- Maintain macOS export via GitHub Actions workflow while local Windows NSIS export artifact is already produced.
- Validate pinned GitHub Actions release path on next tag push and confirm `.zip` updater asset appears beside `.exe`.
- Validate the macOS `aarch64-apple-darwin` workflow run completes and uploads `.app`/`.dmg` assets.
