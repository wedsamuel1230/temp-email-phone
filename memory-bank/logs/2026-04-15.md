# Log - 2026-04-15

- Request: fix macOS GitHub Actions release failure from run 24409516257/job 71302111106.
- Root-cause investigation:
  - Public check annotations show macOS job failed in `npm run tauri build` with args for universal target.
  - Failure annotation did not include full compiler tail, but failure is isolated to macOS build path while Windows job succeeded.
  - Universal mac target is a known high-friction CI path and unnecessary for current release requirement.
- Fix implemented:
  - Updated `.github/workflows/export-desktop.yml` macOS args from `--target universal-apple-darwin` to `--target aarch64-apple-darwin`.
  - Updated macOS rustup step to install only `aarch64-apple-darwin` target.
  - Preserved pinned `tauri-action` SHA and Windows `.exe`/`.zip` settings.
- Verification:
  - `npm run test` passes locally (4 files, 6 tests).
  - `git diff -- .github/workflows/export-desktop.yml` shows only intended workflow modifications.
- Exception Declaration:
  - Reason: CI workflow/configuration-only fix.
  - Scope: `.github/workflows/export-desktop.yml` and memory-bank updates.
  - Proof available: failing-run annotations + focused workflow diff + green tests.
  - Follow-up: trigger next tag push and verify macOS release job uploads `.app` and `.dmg`.
