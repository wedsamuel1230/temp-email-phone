# Log - 2026-04-14

- Request: follow prompt instructions and replace stack text with Tauri + SvelteKit + TypeScript.
- Action: updated stack wording in PRD, validated via grep search.
- Exception Declaration:
  - Reason: config-only/documentation change
  - Scope: PRD wording updates only
  - Proof available: git diff and PRD line checks
  - Follow-up: apply TDD cycle when behavior code changes begin

- Request: execute full scaffold generation from zero using Tauri + SvelteKit + TypeScript.
- Actions:
  - Generated new project structure and configs: package.json, SvelteKit config, Vite config, ESLint config, Tauri Rust shell, README.
  - Added typed service/contracts scaffold and bootstrap UI shell.
  - Added parser utility and tests with RED -> GREEN cycle.
- Verification:
  - RED: `npm run test -- src/lib/utils/phonePoolParser.test.ts` failed before parser implementation.
  - GREEN: same command passed after parser implementation.
  - Additional checks passed: `npm run check`, `npm run lint`, `npm run build`.
- Exception Declaration:
  - Reason: partial scaffold includes baseline utility behavior only, not complete PRD feature implementation.
  - Scope: architecture foundation and bootstrapping.
  - Proof available: generated file tree and successful verification commands.
  - Follow-up: implement full PRD functionality in next phase.

- Request: export project into both mac and windows.
- Attempted local Windows export command: `npm run tauri:build -- --no-sign`.
- Blocker: local `cargo` command not found (Rust toolchain missing), so native bundle generation could not proceed on this machine.
- Resolution implemented:
  - Added CI matrix workflow for native Windows + macOS exports: `.github/workflows/export-desktop.yml`.
  - Added local prerequisite-checked export helper: `scripts/export-local.ps1`.
  - Updated README with export instructions and artifact locations.
- Verification:
  - `npm run check` passes after workflow/script additions.

- Bug follow-up: `d:\projects\node.js\temp-email&phone\scripts\export-local.ps1` failed with `The module 'phone' could not be loaded`.
- Root cause: unquoted absolute PowerShell path contains `&`, which is parsed as command separator.
- Fixes implemented:
  - Added npm alias `export:local` to avoid absolute-path invocation.
  - Updated README with safe invocation patterns (quoted absolute path with call operator).
  - Hardened `scripts/export-local.ps1` to run from any current directory.
- Verification evidence:
  - Reproduced failure with unquoted absolute path.
  - `& "d:\projects\node.js\temp-email&phone\scripts\export-local.ps1"` executes correctly and fails only at expected Cargo prerequisite check.
  - `npm run export:local` executes correctly and fails only at expected Cargo prerequisite check.

- Export completion:
  - Windows bundle successfully produced at `src-tauri/target/release/bundle/nsis/Temp Inbox Manager_0.1.0_x64-setup.exe`.
  - `npm run check` passes after final config updates.
  - macOS bundle remains configured through `.github/workflows/export-desktop.yml` on `macos-latest`.

- Feature implementation follow-up (user requested real functionality):
  - Replaced scaffold placeholder page with working email/SMS app flows:
    - Mail.tm account creation and inbox refresh
    - TXT phone pool import with validation feedback
    - SMS polling and event list/detail
    - Change mode handling (next/random/manual)
    - OTP copy buttons for email and SMS details
  - OTP extraction updated to prioritize 6-digit codes (e.g., `001954`, `675117`) for quick copy.
  - Added tests for OTP extraction and phone change behavior.
- Verification:
  - `npm run test -- src/lib/utils/phoneChange.test.ts src/lib/utils/otp.test.ts src/lib/utils/phonePoolParser.test.ts` passed.
  - `npm run check` passed.
  - `npm run build` passed.

- Follow-up implementation (drag-drop + copy + mac export usability):
  - Added explicit drag-drop import zone in phone panel with visual state.
  - Added copy buttons for phone numbers, email account address, and password.
  - Added macOS bundle config `src-tauri/tauri.macos.conf.json` targeting `app` and `dmg`.
  - Updated README to clarify macOS usable artifact output.
- Verification:
  - `npm run test -- src/lib/utils/phoneChange.test.ts src/lib/utils/otp.test.ts src/lib/utils/phonePoolParser.test.ts` passed.
  - `npm run check` passed.
  - `npm run build` passed.
  - `npm run export:local` passed and produced NSIS installer.

- SMS fetch fix requested by user after Python proof:
  - Added Tauri backend command `fetch_sms_snapshot` so the phone API can be fetched server-side instead of relying only on browser-side fetch.
  - `src/lib/services/smsService.ts` now prefers the Tauri backend and falls back to browser fetch if needed.
  - Added focused regression test for backend SMS fallback using the user's sample payload format.
- Verification:
  - `npm run test -- src/lib/services/smsService.test.ts` passed after fixing the mock payload shape.
  - `npm run export:local` passed again after stopping the locked app process.
  - `npm run check` passed.

- Web-researched workaround for macOS app/dmg generation:
  - Official guidance indicates macOS bundles must be built on macOS host (local Windows build cannot produce `.app`/`.dmg`).
  - Updated workflow to explicitly build macOS bundles with `--bundles app,dmg --target universal-apple-darwin` on `macos-latest`.
  - Added workspace-level drag/drop capture so TXT import does not depend only on the dropzone button target.
- Verification:
  - `npm run check` passed.
  - `npm run build` passed.
  - `npm run export:local` passed after releasing file lock.

- Remove phone + release publishing automation:
  - Added `removePhoneEntry` utility with active-phone fallback selection.
  - Added Remove button in phone list UI and wired it to update persisted pool/selection.
  - Converted export workflow to release workflow that publishes desktop bundles directly to GitHub Releases on tag push (`v*`) using `tauri-apps/tauri-action`.
- Verification:
  - RED: `npm run test -- src/lib/utils/phoneChange.test.ts` failed with `removePhoneEntry is not a function` before implementation.
  - GREEN: `npm run test -- src/lib/utils/phoneChange.test.ts src/lib/services/smsService.test.ts` passed.
  - `npm run check` passed.
  - `npm run build` passed.
  - `npm run export:local` passed.

- Workflow hardening follow-up:
  - Resolved current `tauri-apps/tauri-action@v0` tag commit using `git ls-remote`.
  - Pinned workflow action to immutable SHA `fce9c6108b31ea247710505d3aaaa893ee6768d4`.
  - Retained Windows bundle args for `.exe` + `.zip` (`nsis,updater`) and release body artifact list.
- Verification:
  - `git diff -- .github/workflows/export-desktop.yml` confirms action pin and artifact settings are present.
  - Existing test suite remains passing from prior run (`npm run test`).
